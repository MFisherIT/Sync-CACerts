# Sync-CACerts
Create on-premise Active Directory placeholder computer objects for Entra-Joined devices with the serial numbers of recently issued certificates. Uses local Active Directory Certificate Services, Microsoft Graph (with PowerShell module), Entra ID App Registration (with matching local certificate), &amp; BladeFireLight's Get-IssuedCertificate function.
